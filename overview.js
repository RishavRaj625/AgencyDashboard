// You can extend this script to handle user interactions, sorting, filtering, or anything else
document.addEventListener('DOMContentLoaded', () => {
    console.log('Dashboard loaded');
    // Add any interactivity here
});
